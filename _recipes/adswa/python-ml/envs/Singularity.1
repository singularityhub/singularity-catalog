Bootstrap: docker
From: neurodebian:buster-non-free
%post
    apt-get update -y
    DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
        python3 \
        python3-tk \
        python3-pip
    rm -rf /var/lib/apt/lists/*
    pip3 install pandas numpy scikit-learn scikit-image

