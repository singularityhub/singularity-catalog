Bootstrap: docker
From: python:3.9-buster

%post
  python -m pip install --no-cache \
    numpy \
    pycocotools
  wget https://github.com/mlcommons/inference/archive/refs/heads/r0.5.zip
  unzip r0.5.zip && rm r0.5.zip

%help
  Execution example
  $ python /inference-r0.5/v0.5/classification_and_detection/tools/accuracy-coco.py --mlperf-accuracy-file $OUTDIR/mlperf_log_accuracy.json --coco-dir $DATADIR
