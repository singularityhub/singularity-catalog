Bootstrap: docker
From: python:2.7-slim

%post
    # Update and install required system packages
    apt-get update 

    # Install khmer from PyPI
    pip install --upgrade pip
    pip install "khmer==2.1.1"

    # Clean up
    apt-get clean && rm -rf /var/lib/apt/lists/*


%runscript
    exec "$@"

