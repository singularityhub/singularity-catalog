Bootstrap: docker
From: python:3.11-slim

%labels
    Author thomas cokelaer

%post

    apt update -y && apt install -y git build-essential zlib1g-dev wget


    wget https://github.com/fenderglass/Flye/archive/refs/tags/2.9.3.tar.gz
    tar xvfz 2.9.3.tar.gz
    cd Flye-2.9.3
    python setup.py install
    #cp /Flye/bin/* /usr/local/bin
    cd ../
    rm -rf /Flye

    apt-get autoremove -y
    apt-get clean -y



