Bootstrap: docker
From: broadinstitute/gatk:latest

%setup
    mkdir /scratchLocal
    mkdir /pbtech_mounts
    mkdir /pbtech_mounts/softlib001
    mkdir /pbtech_mounts/oelab_store003
    mkdir /athena
    mkdir /cluster001
